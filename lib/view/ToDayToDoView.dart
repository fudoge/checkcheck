import 'package:flutter/material.dart';

class TodayToDoView extends StatelessWidget {
  const TodayToDoView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
